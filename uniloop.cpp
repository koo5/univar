void loop(State state, Pred* pred, std::vector<Thing *s>)
{
	if(state.comeback)
		goto comeback;
		


for (state.rule_idx = 0; state.rule_idx < pred.size(); state.rule_idx++)
{
	while unify(s, rule.head.s
		while unify(o, rule.head.o
			for bi in rule.body:
				loop(bi.pred, 
	